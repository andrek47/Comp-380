<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.comp380.keeppace">  <!-- 👈 make sure this matches your actual package name -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KeepPace">


        <activity
            android:name="com.comp380.keeppace.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.comp380.keeppace.HomePage"
            android:exported="true"/>

        <activity
            android:name="com.comp380.keeppace.EmailPasswordActivity"
            android:exported="true"/>

        <activity
            android:name="com.comp380.keeppace.UserCreation"
            android:exported="true"/>





    </application>

</manifest>
